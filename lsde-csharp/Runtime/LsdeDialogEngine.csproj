<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>9.0</LangVersion>
    <RootNamespace>LsdeDialogEngine</RootNamespace>
    <AssemblyName>LsdeDialogEngine</AssemblyName>

    <!-- NuGet package metadata -->
    <PackageId>LsdeDialogEngine</PackageId>
    <Version>0.3.0</Version>
    <Authors>LepaSoft</Authors>
    <Description>LSDE Dialog Engine — C# runtime for loading and executing LSDE dialogue blueprints</Description>
    <PackageProjectUrl>https://github.com/jonlepage/LS-Dialog-Editor-Engine</PackageProjectUrl>
    <RepositoryUrl>https://github.com/jonlepage/LS-Dialog-Editor-Engine.git</RepositoryUrl>
    <PackageTags>dialogue;dialog;engine;game;blueprint;lsde</PackageTags>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>

    <!-- Build output outside Runtime/ to avoid Unity importing compiled DLLs -->
    <BaseOutputPath>..\build~\bin\LsdeDialogEngine\</BaseOutputPath>
    <BaseIntermediateOutputPath>..\build~\obj\LsdeDialogEngine\</BaseIntermediateOutputPath>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\LICENSE" Pack="true" PackagePath="\" />
    <None Include="..\README.md" Pack="true" PackagePath="\" />
    <Compile Remove="Newtonsoft\**" />
  </ItemGroup>
</Project>
